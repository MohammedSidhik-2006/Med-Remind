# This file is used by React during production builds (npm run build).
# REACT_APP_ prefixed variables are injected at build time.
# This ensures the deployed Vercel app always calls the correct backend.
REACT_APP_API_URL=https://medi-time-2peh.onrender.com
